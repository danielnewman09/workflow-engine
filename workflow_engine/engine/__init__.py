"""
Workflow Engine core — schema, models, scheduler, claim logic, state machine.

This package is project-agnostic: all project-specific configuration
(phase definitions, agent types, timeouts) comes from the consuming
repository's .workflow/ directory.
"""
